-- 24/7 Workout --
A workout website to track your fitness progress.
Each workout will have a date, category, and details.
Workouts can be update and deleted.

** Run application instructions
run init_db.py 1st (This initializes the categories in the database | can be changed if needed)
run server.py next